spring:
  application:
    name: ws-sesp-service-sic-prontuario
  data:
    mongodb:
      uri: mongodb://localhost:27017/miniautorizador
  datasource:
    url: jdbc:mysql://localhost:3306/miniautorizador?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: dev
server:
  port: 8080